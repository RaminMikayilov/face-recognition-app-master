export const MODEL_URL = '/models';

export const DETECTION_OPTIONS = {
  inputSize: 320,
  scoreThreshold: 0.5,
};

export const DRAW_OPTIONS = {
  boxColor: '#00ff88',
  boxLineWidth: 2,
  labelFont: 'bold 13px system-ui, sans-serif',
  labelColor: '#ffffff',
  labelBgColor: 'rgba(0, 0, 0, 0.65)',
};

export const FPS_SAMPLE_WINDOW = 30;

export const FACE_MATCH_THRESHOLD = 0.6;
